<h3>{{viewTitle}}</h3>

<label>Statut and Success : {{this.statut}} - {{this.success}} </label><br>
<label>Message Result : {{this.message}}</label>
<form action="/arms" method="POST">
    <input type="hidden" name="_id" value="{{arm._id}}">
    <div class="form-row">
        <div class="form-group col-md-6">
            
            <label>Type</label>
            <input type="text" class="form-control" name="Type" value="Arm" readonly>
        </div>
        <div class="form-group col-md-6">
            <label>ID</label>
            <input type="number" class="form-control" name="Id" placeholder="ID" value="{{arm.id}}" readonly>
        </div>
    </div>
    <div class="form-group">
        <label>Name</label>
        <input type="text" class="form-control" name="Name" placeholder="Name your arms" value="{{arm.name}}" required>
    </div>
    <div class="form-group">
        <label>Value </label>
        <input type="number" class="form-control" name="Value" placeholder="Value of your Arm" value="{{arm.value}}" required>
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-info">Envoyer</button>
        <a class="btn btn-secondary" href="/arms/list">View All</a>
    </div>
</form>
<a class="btn btn-primary" href="/">Return to the Project</a>