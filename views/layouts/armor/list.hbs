<h3>Armors List <a class="btn btn-secondary" href="/armors/add">Create a Armor +</a></h3>

<a class="btn btn-primary" href="/">Return to the Project</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Type</th>
            <th>Name</th>
            <th>Value</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {{#each list}}
        <tr>
            <td>{{this.id}}</td>
            <td>{{this.type}}</td>
            <td>{{this.name}}</td>
            <td>{{this.value}}</td>
            <td>
                <a href="/armors/{{this._id}}" aria-hidden="true"></a>
                <a href="/armors/{{this._id}}">Update</a>
                <a href="/armors/delete/{{this._id}}" onclick="return confirm('Are you sure to delete this record ? ');">Delete</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
